spring:
  profiles: mysql
  datasource:
    url: jdbc:mysql://117.17.142.207:3306/real_estate_data?useSSL=false&autoReconnect=true&useUnicode=true&characterEncoding=utf-8
    password: cs616
    username: root
    driver-class-name: com.mysql.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQL5Dialect
  batch:
    job:
      enabled: true
logging:
  level:
    org:
      hibernate: debug
